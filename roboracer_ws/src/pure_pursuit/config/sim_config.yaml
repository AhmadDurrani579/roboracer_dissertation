pure_pursuit:
  ros__parameters:
    waypoints_path: "/home/dev/roboracer_ws/src/pure_pursuit/racelines/levin_blocked.csv"
    odom_topic: "/car_1/odom"
    car_refFrame: "car_1_base_link"
    global_refFrame: "odom"
    drive_topic: "/ackermann_cmd"
    rviz_current_waypoint_topic: "/current_waypoint"
    rviz_lookahead_waypoint_topic: "/lookahead_waypoint"

    # Pure Pursuit Parameters
    # Lookahead is computed using the following: lookahead = min(max(min_lookahead, max_lookahead * curr_velocity / lookahead_ratio), max_lookahead);
    min_lookahead: 0.8
    max_lookahead: 2.5 # lookahead when the car is going around max velocity
    lookahead_ratio: 8.0 # approximately the max velocity
    K_p: 0.3
    steering_limit: 25.0
    velocity_percentage: 0.1 # the limit, can only do 1 lap with clean tires

waypoint_visualizer_node:
  ros__parameters:
    waypoints_path: "/home/dev/roboracer_ws/src/pure_pursuit/racelines/levin_blocked.csv"
